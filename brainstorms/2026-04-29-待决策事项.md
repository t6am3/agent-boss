# Agent Boss 待决策事项 — 2026-04-29

## 问题 1：递归层策略

**选项：**
- **A. 先放一放**（推荐）：MVP 先跑通个人级 4 个 Agent，递归层预留接口但不实现。等运行稳定后再加 parentTaskId / children 字段。
- **B. 同步推进**：现在就把递归字段加进去，但只留空实现。好处是架构一次到位，坏处是增加复杂度。

**建议 A**：递归是锦上添花，不是 MVP 阻塞项。先让路由+评判+对比跑起来。

---

## 问题 2：OpenClaw Gateway WebSocket 协议

**现状**：Adapter 骨架已写，能 health check Gateway（`GET /health` 或 `HEAD /`），但真正的 agent 调用协议（如何 spawn session、发送消息、接收响应）缺文档。

**选项：**
- **A. 直接读 OpenClaw Gateway 源码**：找到 ws message format（JSON 结构、method name、payload 规范）。
- **B. 等 OpenClaw 官方文档**： PROTOCOL.md 或 ABCP 规范。
- **C. 用 REST fallback**：如果 Gateway 有 HTTP API 可以用，先用 REST 实现，再切 WebSocket。

**建议 A + C**：先尝试读 Gateway 源码找 ws 格式，同时看有没有 REST fallback。Gateway 代码在 openclaw 仓库里。

---

## 问题 3：Hermes 怎么接

**现状**：完全不知道 Hermes 是什么。可能是：
1. 字节内部的 Agent 平台（如 PaaS 上的某个服务）
2. 一个开源项目（搜索 hermes agent 无明确结果）
3. 用户自己的某个私有服务

**下一步**：需要用户确认 Hermes 的接入方式（端口 / CLI / API / 文档链接）。

---

## 优先级排序

1. **问题 2（OpenClaw 协议）** → 影响 MVP 可用性，先处理
2. **问题 1（递归层）** → 可延后
3. **问题 3（Hermes）** → 等用户输入

## 当前阻塞

- TypeScript 编译验证：Node 25 + npm 11 + bnpm 镜像导致 `typescript` 包无法安装到 node_modules
- 已修复 3/4 类型错误，剩 `@types/ws` 依赖已声明但未安装
